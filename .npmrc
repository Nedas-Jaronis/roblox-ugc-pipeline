# Refuse to install any npm package version published less than 3 days ago.
# Mitigates fresh supply-chain compromises (e.g. Shai-Hulud / similar worms).
# pnpm and recent npm read this value in MINUTES; 3 days = 4320 minutes.
minimum-release-age=3
